pub mod matchmaker {
    /* TODO
    use native_tls::TlsStream;
    use std::collections::VecDeque;
    use std::net::TcpStream;

    pub struct MatchMaker {
        queue: VecDeque<TlsStream<TcpStream>>,
    }
    impl MatchMaker {
        pub fn new() -> MatchMaker {
            let matchmaker = MatchMaker {
                queue: VecDeque::new(),
            };
            return matchmaker;
        }

        pub fn enqueue(&mut self, client: TlsStream<TcpStream>) {
            self.queue.push_back(client);
        }
    }
    */
}
